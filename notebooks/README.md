# Notebooks

Optional exploratory notebooks can live here. The reproducible project path is script-first:

- `python -m src.extract_features`
- `python -m src.train_classifier`
- `python -m src.evaluate_classifier`
- `python -m src.predict --image path/to/image.jpg --text "product title"`
